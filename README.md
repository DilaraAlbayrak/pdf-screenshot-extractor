# pdf-screenshot-extractor
This repository provides a two-stage Python pipeline to extract pages from DRM-locked PDFs and enhance their visual quality. It bypasses standard PDF restrictions by automating screen captures and subsequently applies image processing and AI upscaling to optimise the output.
